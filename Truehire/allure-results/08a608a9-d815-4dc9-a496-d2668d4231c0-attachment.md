# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api\auth.api.spec.js >> Auth API >> POST /api/auth/login returns JWT for recruiter
- Location: qa\tests\api\auth.api.spec.js:12:3

# Error details

```
Error: {"success":false,"message":"Route not found: POST /auth/login"}

expect(received).toBeLessThan(expected)

Expected: < 300
Received:   404
```

# Test source

```ts
  1  | import { test, expect } from '../../fixtures/test-fixtures.js';
  2  | import { env } from '../../config/env.js';
  3  | 
  4  | test.describe('Auth API', () => {
  5  |   test('POST /api/auth/login returns JWT for admin', async ({ authApi }) => {
  6  |     const response = await authApi.login('admin');
  7  |     expect(response.status(), await response.text()).toBeLessThan(300);
  8  |     const body = await response.json();
  9  |     expect(body.token || body.accessToken || body.jwt || body.data?.token).toBeTruthy();
  10 |   });
  11 | 
  12 |   test('POST /api/auth/login returns JWT for recruiter', async ({ authApi }) => {
  13 |     const response = await authApi.login('recruiter');
> 14 |     expect(response.status(), await response.text()).toBeLessThan(300);
     |                                                      ^ Error: {"success":false,"message":"Route not found: POST /auth/login"}
  15 |   });
  16 | 
  17 |   test('POST /api/auth/login returns JWT for candidate', async ({ authApi }) => {
  18 |     const response = await authApi.login('candidate');
  19 |     expect(response.status(), await response.text()).toBeLessThan(300);
  20 |   });
  21 | 
  22 |   test('invalid credentials are rejected', async ({ authApi }) => {
  23 |     const response = await authApi.login('candidate', { email: env.credentials.candidate.email, password: 'bad-password' });
  24 |     expect([400, 401, 403, 404, 422]).toContain(response.status());
  25 |   });
  26 | 
  27 |   test('missing required fields return validation error', async ({ apiClient }) => {
  28 |     const response = await apiClient.post('/auth/login', { email: '' });
  29 |     expect([400, 401, 422]).toContain(response.status());
  30 |   });
  31 | });
  32 | 
```